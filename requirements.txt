# Core
fastapi==0.115.6
uvicorn[standard]==0.34.0
starlette==0.41.3
gunicorn==23.0.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-multipart==0.0.20

# Text Extraction
PyPDF2==3.0.1
pdfplumber==0.11.9
python-docx==1.1.2
python-pptx==1.0.2
fpdf2==2.8.3

# Async
aiofiles==24.1.0
httpx==0.28.1

# Web & Academic Search
ddgs>=9.0.0

# Image processing
Pillow>=10.0.0

# Embeddings & ML
sentence-transformers>=3.0.0
numpy>=1.26.0

# Authentication
PyJWT==2.9.0
google-auth==2.35.0

# Payments
razorpay==1.4.2
stripe>=8.0.0
# Pinned because razorpay 1.4.2 imports `pkg_resources`, which was removed
# in setuptools 81+. Drop this pin once razorpay ships a release that uses
# importlib.metadata instead.
setuptools>=75.0.0,<81

# Database (Azure SQL)
pyodbc==5.2.0

# Azure Communication Services (Email)
azure-communication-email>=1.0.0

# Logging
structlog==24.4.0

# Runtime
python-dotenv==1.0.1

# Testing
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-timeout==2.4.0

# Agent Rules

* Each agent must be independent and reusable
* All agents must return structured JSON outputs
* Do not mix business logic with API routes
* Keep tool logic separate from agent logic
* Prefer async functions for all IO operations
* Ensure consistent input/output schema across agents
* Add logging for each agent execution
* Avoid hardcoding values
* Keep outputs deterministic and testable
